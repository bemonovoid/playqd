server:
  port: 9585

spring:
  datasource:
    driver-class-name: "org.hsqldb.jdbc.JDBCDriver"
    url: "jdbc:hsqldb:hsql://127.0.0.1/playqd_db"
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    propersties:
      hibernate:
        jdbc:
          batch_size: 100

playqd:
  library:
    location: "/app/library"
#    location: "D:/Music"
logging:
  level:
    web: DEBUG
